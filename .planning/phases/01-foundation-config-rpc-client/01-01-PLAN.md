---
phase: 01-foundation-config-rpc-client
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .gitignore
  - .env.example
  - bastion/__init__.py
  - bastion/rpc/__init__.py
  - bastion/rpc/errors.py
  - tests/__init__.py
  - tests/unit/__init__.py
  - tests/conftest.py
  - tests/unit/test_harness_smoke.py
autonomous: false
requirements: [CLI-05, CLI-06]
user_setup: []

must_haves:
  truths:
    - "The project builds and its test suite runs (`uv run pytest` collects and passes) from a clean checkout"
    - "`.env.example` documents every CLI-05 env var and every CLI-06 safety rail; `.gitignore` keeps `.env` and the keystore dir out of version control"
    - "A shared test harness (respx httpx.AsyncClient mock factory + local websockets.serve() server with a forced silent-drop hook) is importable and usable by every downstream test module"
    - "A hash-pinned uv.lock exists so dependency resolution is reproducible (supply-chain posture, D-05/DIST habit)"
  artifacts:
    - path: "pyproject.toml"
      provides: "hatchling build backend, deps, pytest asyncio_mode"
      contains: "hatchling"
    - path: ".env.example"
      provides: "documents every env var + safety rail"
      contains: "SOLANA_RPC"
    - path: ".gitignore"
      provides: "keeps secrets and keystore out of VCS"
      contains: ".env"
    - path: "bastion/rpc/errors.py"
      provides: "typed RPC error hierarchy shared by client + ws"
      contains: "class RpcError"
    - path: "tests/conftest.py"
      provides: "respx httpx mock factory + local WS test server with silent-drop hook"
      contains: "def"
  key_links:
    - from: "tests/unit/*.py"
      to: "tests/conftest.py"
      via: "pytest fixture injection (respx factory + ws server harness)"
      pattern: "def test_"
---

<objective>
Establish the greenfield project scaffolding and the shared test harness that all Phase 1 feature plans (01-02 config, 01-03 rpc/client, 01-04 rpc/ws) build on. This is the phase's Wave 0: nothing testable exists yet — no `pyproject.toml`, no package layout, no `uv.lock`, no test framework. This plan creates all of it.

Purpose: Deliver a buildable, testable, reproducible foundation so the three Wave 2 feature plans can run in parallel, each writing only its own module + test file against a stable, mockable harness. Establishes the httpx (not requests) + modern websockets stack, hatchling/uv packaging, and the hash-pinned lockfile habit from setup.

Output (symbols/artifacts this plan creates):
- `pyproject.toml` (hatchling backend, `[project]` deps: httpx, websockets, python-dotenv; dev deps: pytest, pytest-asyncio, respx; `[tool.pytest.ini_options]` `asyncio_mode="auto"`)
- `uv.lock` (hash-pinned)
- `.gitignore`, `.env.example` (documents every CLI-05 env var + CLI-06 safety rail)
- `bastion/__init__.py`, `bastion/rpc/__init__.py` (package markers)
- `bastion/rpc/errors.py` — typed error classes `RpcError`, `RpcRateLimitError`, `RpcTimeoutError`
- `tests/__init__.py`, `tests/unit/__init__.py`
- `tests/conftest.py` — shared fixtures: a respx-mocked `httpx.AsyncClient` factory, and a local `websockets.serve()` test-server fixture exposing hooks for a forced **silent drop** (pause reads/writes without sending a close frame), a clean close, and captured subscribe messages
- `tests/unit/test_harness_smoke.py` — proves the harness fixtures load and round-trip
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/01-foundation-config-rpc-client/01-CONTEXT.md
@.planning/phases/01-foundation-config-rpc-client/01-RESEARCH.md
@.planning/phases/01-foundation-config-rpc-client/01-VALIDATION.md

<interfaces>
<!-- Contracts this plan CREATES that 01-02/01-03/01-04 consume. Downstream plans import these directly — no exploration needed. -->

bastion/rpc/errors.py exports (typed exception hierarchy):
- class RpcError(Exception)                       # base for all transport failures
- class RpcRateLimitError(RpcError)               # raised when the 429/backoff budget is exhausted
- class RpcTimeoutError(RpcError)                 # raised on request/liveness timeout

tests/conftest.py exposes (pytest fixtures, asyncio_mode="auto"):
- a respx-backed factory fixture that yields an httpx.AsyncClient bound to a fixed base_url with respx intercepting POSTs (used by 01-03)
- a local websockets.serve() server fixture that: accepts a connection, records every inbound subscribe payload for assertion, and exposes control hooks to (a) force a SILENT drop = stop reading/writing WITHOUT sending a close frame, (b) perform a clean close, (c) push a server message to the client (used by 01-04)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate (pre-install)</name>
  <files>pyproject.toml (not yet written — this gate precedes it)</files>
  <action>PAUSE before any dependency install. Do not run `uv add` / `uv sync` until the human has confirmed the six package names below against PyPI. This is a blocking human-verify gate (not auto-approvable, even under auto_advance) required because RESEARCH.md ## Package Legitimacy Audit flagged all six deps `SUS` on a single `unknown-downloads` signal.</action>
  <what-built>Nothing yet — this gate precedes the first `uv add`. RESEARCH.md ## Package Legitimacy Audit flagged all six dependencies as `SUS` on a single `unknown-downloads` signal (this environment has no live download-telemetry feed), then cross-verified each via `pip index versions` and confirmed the official GitHub repos. Per GSD policy, `SUS`-verdict packages require a blocking human confirmation before install; the researcher's endorsed compromise is ONE consolidated checkpoint (not one per package).</what-built>
  <how-to-verify>
    Confirm each package name resolves to its well-known official project on PyPI before install:
    1. https://pypi.org/project/httpx/ (repo github.com/encode/httpx)
    2. https://pypi.org/project/websockets/ (repo github.com/python-websockets/websockets)
    3. https://pypi.org/project/python-dotenv/ (repo github.com/theskumar/python-dotenv)
    4. https://pypi.org/project/respx/ (repo github.com/lundberg/respx)
    5. https://pypi.org/project/pytest/ (repo github.com/pytest-dev/pytest)
    6. https://pypi.org/project/pytest-asyncio/ (repo github.com/pytest-dev/pytest-asyncio)
    All six are among the most-used packages in the Python ecosystem and were pre-selected in .planning/research/STACK.md.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to install, or name any package to hold.</resume-signal>
  <verify>Human confirms all six package names resolve to their official PyPI projects before install proceeds.</verify>
  <done>Human approval recorded; install (Task 2) may run.</done>
</task>

<task type="auto">
  <name>Task 2: Project scaffold — pyproject.toml, deps, lockfile, ignores, package skeleton</name>
  <files>pyproject.toml, uv.lock, .gitignore, .env.example, bastion/__init__.py, bastion/rpc/__init__.py, bastion/rpc/errors.py, tests/__init__.py, tests/unit/__init__.py</files>
  <read_first>.planning/phases/01-foundation-config-rpc-client/01-RESEARCH.md (## Standard Stack version table + ## Validation Architecture Wave 0 Gaps), .claude/CLAUDE.md (Technology Stack table — httpx not requests, modern websockets, hatchling, uv)</read_first>
  <action>
    Create `pyproject.toml` with the `hatchling` build backend and `requires-python = ">=3.11"`. Declare runtime deps `httpx` (>=0.28,<0.29), `websockets` (>=16,<17), `python-dotenv` (>=1.2,<2). Add a dev dependency group containing `pytest`, `pytest-asyncio`, `respx`. Add `[tool.pytest.ini_options]` with `asyncio_mode = "auto"` and `testpaths = ["tests"]`. Configure hatchling wheel packaging to include the `bastion` package (flat layout, NOT `src/`, per the planning-context greenfield note). Set project name `bastion` and a `0.0.1` version.
    Install and lock with uv: run `uv add httpx websockets python-dotenv` then `uv add --dev pytest pytest-asyncio respx`, which writes the resolved, hash-pinned `uv.lock`. If `uv add` is unavailable, declare the deps in `pyproject.toml` manually and run `uv sync` to generate `uv.lock`. The lockfile MUST be produced and committed — reproducible installs are the DIST/supply-chain habit from setup.
    Create `.gitignore` covering at minimum: `.env`, `.venv/`, `__pycache__/`, `*.pyc`, `.pytest_cache/`, the keystore directory pattern (a `keystore/` / `*.keystore` entry), and `*.db` (future SQLite store). Do NOT ignore `uv.lock` or `.env.example`.
    Create `.env.example` documenting EVERY variable with placeholder values and a one-line comment each: CLI-05 vars `SOLANA_RPC`, `SOLANA_WS`, `VAULT_SECRET`, `VAULT_PUBKEY`, `KEYSTORE_DIR`, `KEYSTORE_PASSPHRASE`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `PUSHOVER_TOKEN`, `PUSHOVER_USER`; CLI-06 safety rails `MAX_SESSION_CAP`, `FEE_RESERVE_LAMPORTS`, `SCORE_WATCH_THRESHOLD`, `SCORE_CRITICAL_THRESHOLD`. `.env.example` MUST contain only placeholder / example values, never a real secret. Note next to `KEYSTORE_PASSPHRASE` that leaving it unset triggers an interactive getpass prompt (per D-02).
    Create empty package-marker files `bastion/__init__.py`, `bastion/rpc/__init__.py`, `tests/__init__.py`, `tests/unit/__init__.py`. Establish `rpc/` as a package (client.py + ws.py separable, per D-04), keeping `bastion/rpc/__init__.py` a bare marker (no re-exports) so 01-03 and 01-04 can add their modules without touching this file — downstream imports come from `bastion.rpc.client` / `bastion.rpc.ws` directly.
    Create `bastion/rpc/errors.py` defining the typed exception hierarchy: `RpcError(Exception)` as the base, `RpcRateLimitError(RpcError)` for exhausted-retry-budget, and `RpcTimeoutError(RpcError)` for request/liveness timeouts. Each carries an optional message; no secrets in any error string. These are the shared error types both 01-03 (client) and 01-04 (ws) raise.
  </action>
  <acceptance_criteria>
    - `uv run python -c "import bastion, bastion.rpc, bastion.rpc.errors"` exits 0
    - `bastion.rpc.errors.RpcRateLimitError` and `RpcTimeoutError` are subclasses of `RpcError`
    - `pyproject.toml` names the hatchling backend and sets `asyncio_mode` to auto
    - `uv.lock` exists and is non-empty (hash-pinned resolution present)
    - `.gitignore` contains a `.env` entry; `.env.example` contains all 14 documented variable names above
    - `.env.example` contains no real secret values (placeholders only)
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import bastion.rpc.errors as e; assert issubclass(e.RpcRateLimitError, e.RpcError) and issubclass(e.RpcTimeoutError, e.RpcError)" && test -s uv.lock && grep -q 'SOLANA_RPC' .env.example && grep -qx '.env' .gitignore</automated>
  </verify>
  <done>Project imports cleanly, uv.lock is generated, ignore/example files present and correct, typed error hierarchy defined.</done>
</task>

<task type="auto">
  <name>Task 3: Shared test harness — respx factory + local WS server + smoke test</name>
  <files>tests/conftest.py, tests/unit/test_harness_smoke.py</files>
  <read_first>.planning/phases/01-foundation-config-rpc-client/01-RESEARCH.md (## Validation Architecture + Pattern 6 WS heartbeat/reconnect + ## Code Examples respx sections), .planning/phases/01-foundation-config-rpc-client/01-VALIDATION.md (Wave 0 Requirements)</read_first>
  <action>
    Create `tests/conftest.py` with two reusable fixtures the downstream feature plans consume:
    (1) A respx-based factory that yields an `httpx.AsyncClient` bound to a fixed test base_url (e.g. `https://rpc.test/`) with a respx router active, so 01-03's tests can queue sequenced responses via `side_effect=[...]` (including 429-with-`Retry-After` then 200). Expose the router/mock so a test can assert `call_count`.
    (2) A local WebSocket test server fixture built on `websockets.serve()` bound to an ephemeral localhost port, yielding the connectable `ws://` URI plus a control handle. The handle MUST support: recording every inbound subscribe payload (so 01-04 can assert re-subscription happened), a `force_silent_drop()` hook that stops the server reading/writing WITHOUT sending a close frame (simulates the NAT/idle black-hole failure mode — the key distinction from a clean close), a `clean_close()` hook that sends a normal close frame, and a `push(message)` hook to send a server→client message. Use the modern `websockets.asyncio` server API, never `websockets.legacy`.
    Keep both fixtures async-friendly (asyncio_mode="auto" is set). Do not import any `bastion.rpc` client/ws module here — the harness is transport-agnostic infrastructure so it can be built before those modules exist.
    Create `tests/unit/test_harness_smoke.py` proving the harness works end-to-end: one test drives the respx factory (a mocked POST returns a canned JSON body and the client receives it), and one async test connects a raw `websockets` client to the WS server fixture, sends a payload, asserts the server recorded it, then invokes `force_silent_drop()` and asserts the client observes no clean close frame (the read hangs / the connection goes quiet rather than raising a normal close) within a short timeout.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_harness_smoke.py -q` passes
    - The respx fixture lets a test intercept a POST and read a canned response
    - The WS server fixture records inbound subscribe payloads and exposes `force_silent_drop`, `clean_close`, and `push` hooks
    - `force_silent_drop()` produces a quiet connection (no close frame delivered), distinguishable from `clean_close()`
    - conftest imports no `bastion.rpc.client` or `bastion.rpc.ws` module
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_harness_smoke.py -x -q</automated>
  </verify>
  <done>Shared harness fixtures load, round-trip through respx and the local WS server, and the silent-drop hook is proven distinct from a clean close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment (`.env` / OS env) → Local Process | Config + secret material enters the process at startup; a committed or world-readable `.env` leaks it |
| Developer machine → PyPI (dependency supply chain) | Installed packages execute with full local privileges on a fund-moving tool — a poisoned dependency is a drainer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env` / `.gitignore` / `.env.example` | high | mitigate | `.gitignore` lists `.env` and the keystore dir; `.env.example` ships placeholders only (never a real secret); acceptance test asserts `.env` is git-ignored and `.env.example` contains no secret values |
| T-01-02 | Denial of Service | committed `*.db` / keystore artifacts bloating or leaking | low | accept | Single-user local tool; `.gitignore` covers `*.db` and keystore dir as defense-in-depth; residual risk (user force-adds) is out of scope to prevent |
| T-01-SC | Tampering | uv/pip installs (httpx, websockets, python-dotenv, respx, pytest, pytest-asyncio) | high | mitigate | Blocking `checkpoint:human-verify` (Task 1) confirms all six package names on PyPI before `uv add`; hash-pinned `uv.lock` committed for reproducible verification; RESEARCH.md ## Package Legitimacy Audit cross-verified each repo |
</threat_model>

<artifacts_produced>
- Files: `pyproject.toml`, `uv.lock`, `.gitignore`, `.env.example`, `bastion/__init__.py`, `bastion/rpc/__init__.py`, `bastion/rpc/errors.py`, `tests/__init__.py`, `tests/unit/__init__.py`, `tests/conftest.py`, `tests/unit/test_harness_smoke.py`
- Symbols: `RpcError`, `RpcRateLimitError`, `RpcTimeoutError` (typed error hierarchy); respx `httpx.AsyncClient` factory fixture; local `websockets.serve()` test-server fixture with `force_silent_drop`/`clean_close`/`push`/subscribe-capture hooks
- Env vars documented: SOLANA_RPC, SOLANA_WS, VAULT_SECRET, VAULT_PUBKEY, KEYSTORE_DIR, KEYSTORE_PASSPHRASE, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID, PUSHOVER_TOKEN, PUSHOVER_USER, MAX_SESSION_CAP, FEE_RESERVE_LAMPORTS, SCORE_WATCH_THRESHOLD, SCORE_CRITICAL_THRESHOLD
</artifacts_produced>

<verification>
- `uv run pytest tests/unit/ -q` collects and passes (only the smoke test exists at this wave)
- `uv run python -c "import bastion.rpc.errors"` succeeds
- `uv.lock` present and hash-pinned; `.env`/keystore ignored; `.env.example` complete
- Package legitimacy checkpoint approved before any install ran
</verification>

<success_criteria>
Buildable, testable, reproducible greenfield foundation: hatchling/uv project with a hash-pinned lockfile, a documented `.env.example` covering every CLI-05 var and CLI-06 rail, secret-safe `.gitignore`, a typed RPC error hierarchy, and a shared respx + local-WS-server harness that 01-02/01-03/01-04 build on without modification.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-config-rpc-client/01-01-SUMMARY.md` when done.
</output>
